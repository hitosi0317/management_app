<div class="wrapper">

<div class="training today">
  <h2 class="training text">本日のトレーニング項目です</h2>
</div>
<% if @motion == nil%>
 <% else %>
<div class="contents row">
  <div class="training-list">
  <%= @motion.training1 %>
  <%= @motion.count1  %>
  </div> 
<% if @motion.training2.present?%>
  <div class="training-list">
  <%= @motion.training2  %>
  <%= @motion.count2  %>
  </div> 
<% end%>
<% if @motion.training3.present?%>
  <div class="training-list">
  <%= @motion.training3  %>
  <%= @motion.count3  %>
  </div>
<% end %>
<% if @motion.training4.present?%>
  <div class="training-list">
  <%= @motion.training4  %>
  <%= @motion.count4  %> 
  </div>
<% end %>
 <% if @motion.training5.present?%> 
  <div class="training-list">
  <%= @motion.training5  %>
  <%= @motion.count5  %>  
  </div>
</div>
<% end %>
<div class="whight-right">
  <div class="whight">
  <h3><%=@motion.user.nickname%>さんの現在の体重</h3>
  <%= @motion.weight %>
  <h3>BMIチエック 肥満度 標準体重の目安<h3>
  <h3>現在の<%=@motion.user.nickname%>さんのBMIは<%= @motion.weight / (@motion.user.height / @motion.user.height)%>
  </div>
</div>

<% end %>
<%if @motion != nil%>
 <%= link_to "編集する", edit_motion_path(@motion.id),class:"motion-btn"%>
 <%= link_to "削除", motion_path(@motion.id), method: :delete, class:"motion-btn" %>
<% end %>

</div>